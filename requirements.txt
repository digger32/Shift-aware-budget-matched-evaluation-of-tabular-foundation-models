# Pinned environment used to produce the reported results
# (Python 3.12, CUDA 12.4). Install: pip install -r requirements.txt
# tableshift is not distributed on PyPI; install it separately as a data
# loader:  pip install --no-deps git+https://github.com/mlfoundations/tableshift
# together with:  pip install --only-binary :all: xport==3.6.1 fairlearn \
#     datasets frozendict category_encoders h5py tables openpyxl
numpy==1.26.4
pandas==2.2.3
scipy==1.14.1
scikit-learn==1.5.2
xgboost==2.1.3
lightgbm==4.5.0
catboost==1.2.10
optuna==4.1.0
scikit-posthocs==0.11.1
matplotlib==3.9.3
pyyaml==6.0.2
torch==2.5.1
tabpfn==8.1.0
tabicl==2.1.1
huggingface_hub==0.27.0
relplot==1.0.3
folktables==0.0.12
openml==0.15.0
